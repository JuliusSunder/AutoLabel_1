{
  "buildCommand": "prisma migrate resolve --rolled-back 20251229110155_make_stripe_customer_id_nullable && prisma migrate resolve --rolled-back 20251229110641_add_password_reset_token && prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install && npx prisma generate"
}

